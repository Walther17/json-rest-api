import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'JSON REST API G5',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
